# Spotrz (Frontend)

Vite + React UI for the Realtime Sports Broadcast backend.

## Run

From the repo root:

```bash
npm run fe:install
npm run fe:dev
```

The frontend expects the backend at `http://localhost:8000`.
If you need to change it, update `.env` in this folder.
